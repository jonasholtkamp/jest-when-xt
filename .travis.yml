language: node_js

node_js:
- node

cache:
  directories:
  - node_modules

before_script:
- npm install --global codecov

script:
- npm run standard
- npm test
- codecov

deploy:
  provider: npm
  email: "$NPM_EMAIL"
  api_key: "$NPM_TOKEN"
  skip_cleanup: true
  on:
    tags: true
